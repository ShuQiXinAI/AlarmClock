---
title: 叫不醒你不罢休 隐私政策
permalink: /PRIVACY/
---

# 叫不醒你不罢休 隐私政策

**最后更新日期：2026-05-01**
**生效日期：2026-05-01**

> 本应用（应用名"叫不醒你不罢休"，包名 `com.shuqixin.alarmclock`，下文简称"本 App"）由开发者个人开发并发布。在你使用本 App 前，请认真阅读本《隐私政策》。继续使用本 App 即表示你已知悉并同意本政策的全部内容。

---

## 一、政策摘要（先读这一段）

- ✅ 本 App **不收集、不上传、不分享**任何个人信息到外部服务器
- ✅ 所有数据（闹钟设置、统计数据）**仅存储在你本地手机**
- ✅ 本 App **不接入任何第三方广告 SDK / 数据分析 SDK / 推送 SDK**
- ✅ 摄像头、闹钟、通知等敏感权限**全部在本地处理**，不离开你的设备
- ✅ 卸载本 App 即代表完全删除所有相关数据

---

## 二、本 App 收集与存储的信息

### 1. 不收集的信息
本 App **不收集**以下任何信息：
- 你的姓名、电话、邮箱、身份证号等个人身份信息
- 你的位置信息（GPS / WiFi / 基站）
- 你的通讯录、短信、通话记录
- 你的图片、视频、文档
- 你的设备识别码（IMEI / Android ID / 广告 ID）
- 任何用于交叉识别的标识

### 2. 仅在你设备本地存储的信息

| 数据类别 | 内容 | 存储位置 | 离开过设备吗 |
| --- | --- | --- | --- |
| 闹钟设置 | 时间、标签、重复星期、解锁方式、启用状态 | 本地 AsyncStorage | ❌ |
| 起床统计 | 起床打卡次数、连续天数 | 本地 AsyncStorage | ❌ |
| App 本地偏好 | UI 设置等（如有） | 本地 AsyncStorage | ❌ |

以上数据**仅写入你手机的应用沙盒**，开发者无法访问，**也不存在任何远程服务器**。

---

## 三、本 App 申请的权限及用途

为实现核心闹钟功能，本 App 需向系统申请以下权限。每项权限**仅在对应功能被调用时使用**，且数据全部在本地处理。

### 必需权限

| 权限 | 用途 | 数据走向 |
| --- | --- | --- |
| `POST_NOTIFICATIONS`（通知） | 闹钟时间到时弹出通知 | 本地系统通知服务 |
| `USE_EXACT_ALARM` / `SCHEDULE_EXACT_ALARM`（精确闹钟） | 让闹钟在精确时间触发，不受省电模式影响 | 本地 AlarmManager |
| `USE_FULL_SCREEN_INTENT`（全屏意图） | 闹铃响起时，即使锁屏也能弹出挑战界面 —— 这是闹钟应用的核心功能 | 本地 |
| `RECEIVE_BOOT_COMPLETED`（开机自启） | 手机重启后恢复你已设置但尚未触发的闹钟 | 本地 BroadcastReceiver |
| `WAKE_LOCK`（唤醒锁） | 闹铃响起时唤醒屏幕 | 本地 |
| `VIBRATE`（震动） | 闹铃响起时震动手机 | 本地 |
| `FOREGROUND_SERVICE`（前台服务） | 让闹铃在用户解锁挑战期间可靠地持续播放 | 本地 |
| `MODIFY_AUDIO_SETTINGS`（音频设置） | 在闹铃响起时，若闹钟音量为 0 则自动恢复一个最低可听音量，避免响不出来 | 本地 AudioManager |

### 可选权限（仅在使用对应解锁方式时调用）

| 权限 | 用途 | 数据走向 |
| --- | --- | --- |
| `CAMERA`（摄像头） | 仅在你**主动选择"眨眼"解锁方式**时启用，用于检测你是否完成连续眨眼 | **图像帧仅在本地处理**，**不录制、不保存、不上传**，挑战完成后立即丢弃 |
| `RECORD_AUDIO`（麦克风） | 摄像头模块默认携带的权限声明。本 App 实际**不录制任何音频内容**，建议使用前到系统设置中**禁用此权限**也不影响任何功能 | 不使用 |

### 你可以随时关闭权限

你可以在系统"**设置 → 应用 → 叫不醒你不罢休 → 权限**"中关闭任何权限。关闭后，对应功能会失效（例如关闭摄像头权限会无法使用眨眼挑战），但**不影响其他闹钟功能**。

---

## 四、第三方 SDK 说明

本 App **不接入任何第三方 SDK**，包括但不限于：
- ❌ 不接入广告 SDK（如穿山甲、优量汇等）
- ❌ 不接入数据分析 SDK（如友盟、神策、Firebase Analytics 等）
- ❌ 不接入推送 SDK（如个推、极光等）
- ❌ 不接入登录 / 分享 SDK（如微信、微博等）
- ❌ 不接入支付 SDK

本 App 使用的开源框架（如 React Native、Expo、Notifee）均为构建运行时基础库，**不在运行时上报任何数据**。

---

## 五、数据安全

- 所有本地数据存储在 Android 应用沙盒中，受系统级隔离保护，其他 App **无法读取**
- 卸载本 App 时，所有本地数据**立即被系统清除**，无任何残留
- 本 App **没有云端备份功能**，所以不存在云端数据泄露风险

---

## 六、你的权利

你随时可以行使以下权利，无需任何额外操作：

- **查看 / 修改数据**：在 App 内直接查看、新增、编辑、删除任何闹钟
- **删除全部数据**：卸载本 App 即可删除所有相关数据
- **撤回权限授权**：随时在系统设置中关闭任意权限
- **拒绝服务**：如不同意本政策，请直接卸载本 App

---

## 七、儿童隐私

本 App 不针对 14 周岁以下儿童设计，也不会主动收集任何儿童信息。如监护人发现儿童在未经同意的情况下使用本 App，请直接卸载即可，本 App 本身不存留任何信息。

---

## 八、政策变更

如本政策发生重要变更（如新增数据收集行为），开发者会在本仓库（GitHub）以及应用商店内公告。**继续使用即表示你接受变更后的政策**；如不接受，请停止使用并卸载。

历史版本可在本 App GitHub 仓库的 git 历史中查阅：
https://github.com/ShuQiXinAI/AlarmClock/commits/master/PRIVACY.md

---

## 九、联系开发者

如对本政策有任何疑问，或希望举报隐私问题、行使你的权利，请通过以下方式联系：

- **GitHub Issues**（推荐）：https://github.com/ShuQiXinAI/AlarmClock/issues

---

## 附：开源信息

本 App 的完整源代码托管于：
https://github.com/ShuQiXinAI/AlarmClock

你可在仓库中审计任何收集行为是否与本政策一致。
